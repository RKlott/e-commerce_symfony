{% extends 'base.html.twig' %}

{% block title %}
    Edit City
{% endblock %}

{% block body %}

    <div class="container">
        <h1>Edit City</h1>

        {{ include('city/_form.html.twig', {'button_label': 'Enregistrer'}) }}

        <a href="{{ path('app_city_index') }}" class="btn btn-light">Retour aux villes</a>

        {{ include('city/_delete_form.html.twig') }}
    </div>
{% endblock %}
