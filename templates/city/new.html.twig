{% extends 'base.html.twig' %}

{% block title %}New City{% endblock %}

{% block body %}
<div class="container">
    <h1>Create new City</h1>

    {{ include('city/_form.html.twig') }}

    <a href="{{ path('app_city_index') }}" class="btn btn-light ">Retour à la liste des villes</a>
    </div>
{% endblock %}
